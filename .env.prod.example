# ============================================
# PyLucy - Configuración de PRODUCCIÓN
# ============================================
# Este archivo es una plantilla. Para usar:
#   1. Copiar: cp .env.prod.example .env.prod
#   2. Editar .env.prod con valores reales
#   3. NO subir .env.prod a Git (está en .gitignore)

# ============================================
# DJANGO
# ============================================
DJANGO_DEBUG=False
DJANGO_ENV=production
SECRET_KEY=CAMBIAR_POR_CLAVE_SECRETA_GENERADA
ALLOWED_HOSTS=tu-dominio.com,www.tu-dominio.com,servidor.unrc.edu.ar

# #ETME - Modo testing/producción
# Valores: 'testing' o 'production'
# testing: usa prefijo "test-a" para cuentas Teams
# production: usa prefijo "a" para cuentas Teams
ENVIRONMENT_MODE=production

# Prefijo para cuentas (depende de ENVIRONMENT_MODE)
# En testing: test-a
# En producción: a
ACCOUNT_PREFIX=a

# ============================================
# BASE DE DATOS
# ============================================
DB_ENGINE=django.db.backends.postgresql
DB_NAME=pylucy
DB_USER=pylucy
DB_PASSWORD=CAMBIAR_CONTRASEÑA_POSTGRESQL_SEGURA
DB_HOST=db
DB_PORT=5432

# ============================================
# SIAL/UTI API
# ============================================
# URL del servicio SIAL real en producción
SIAL_BASE_URL=https://sial.unrc.edu.ar
SIAL_BASIC_USER=usuario_sial_produccion
SIAL_BASIC_PASS=contraseña_sial_produccion

# ============================================
# MOODLE
# ============================================
# URL base de Moodle en producción
MOODLE_BASE_URL=https://moodle.eco.unrc.edu.ar
MOODLE_WSTOKEN=token_moodle_webservices_produccion

# ============================================
# EMAIL (SMTP)
# ============================================
# Servidor SMTP institucional
EMAIL_HOST=smtp.eco.unrc.edu.ar
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=no-reply@eco.unrc.edu.ar
EMAIL_HOST_PASSWORD=contraseña_smtp_produccion
DEFAULT_FROM_EMAIL=no-reply@eco.unrc.edu.ar

# ============================================
# MICROSOFT TEAMS / AZURE AD
# ============================================
# Credenciales de App Registration en Azure AD
TEAMS_TENANT=GUID_del_tenant_azure_ad
TEAMS_DOMAIN=eco.unrc.edu.ar
TEAMS_CLIENT_ID=GUID_del_client_id
TEAMS_CLIENT_SECRET=secreto_del_client

# ============================================
# CELERY (Redis)
# ============================================
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# ============================================
# SEGURIDAD
# ============================================
# Configuración HTTPS (cuando tengas certificado SSL)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# ============================================
# STATIC FILES
# ============================================
STATIC_ROOT=/app/staticfiles
STATIC_URL=/static/

# ============================================
# LOGGING
# ============================================
# Nivel de logs (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================
# NOTAS DE CONFIGURACIÓN
# ============================================
#
# Para generar SECRET_KEY segura:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# Para obtener credenciales de Teams/Azure AD:
#   1. Ir a portal.azure.com
#   2. Azure Active Directory > App registrations
#   3. Crear nueva app o usar existente
#   4. Copiar: Application (client) ID y Directory (tenant) ID
#   5. Certificates & secrets > New client secret
#
# Para obtener token de Moodle:
#   1. Admin Moodle > Site administration > Plugins > Web services
#   2. Enable web services
#   3. Create service user
#   4. Generate token
#
