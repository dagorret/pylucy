1)
python -m venv venv


2)
source venv/bin/activate

3)
pip install -r requirements.txt


4)
Si ya tienes paquetes instalados en un entorno activo y quieres crear el archivo requirements.txt para compartir el proyecto, ejecuta el siguiente comando:
bash

pip freeze > requirements.txt


5)
deactivate

